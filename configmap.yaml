apiVersion: v1
kind: ConfigMap
metadata:
  name: my-configmap
  labels:
    app: myapp

data:
  mysql_root_password: root
  MYSQL_USER: admin
  MYSQL_PASSWORD: admin
  MYSQL_DATABASE: student_db #apply